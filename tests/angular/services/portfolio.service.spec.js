ngDescribe({
    name: 'Test PortfolioService',
    modules: 'app',
    inject: 'PortfolioService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
